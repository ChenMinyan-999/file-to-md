# 标准文档转 Markdown 部署与运行流程

本文档记录当前 MinerU + `convert_to_md.py` 的完整部署流程、每次运行前需要的指令，以及常见问题处理。

---

## 一、当前环境信息

| 项目 | 当前值 |
|---|---|
| 项目目录 | `D:\chemical_design\test\standrad` |
| 源文档目录 | `D:\chemical_design\test\standrad\标准2` |
| 默认输出目录 | `D:\chemical_design\test\standrad\标准2_md` |
| 虚拟环境 | `D:\chemical_design\test\standrad\.venv` |
| Python | 3.12.12 |
| MinerU | 4.0.5 |
| MinerU 模型目录 | `D:\mineru\models` |
| MinerU 主目录 | `C:\Users\Administrator\.mineru` |
| 本地 Parse Server | `http://127.0.0.1:16580`，tier=standard |
| doclib 服务 | `http://127.0.0.1:15980` |

> 重要：项目路径必须是全英文/ASCII，否则 `mineru_llama_cpp` 可能报：
>
> `RuntimeError: No mapping for the Unicode character exists in the target multi-byte code page.`
>
> 当前路径 `D:\chemical_design\test\standrad` 已满足要求。

---

## 二、首次部署流程

### 1. 准备目录

推荐结构：

```text
D:\chemical_design\test\standrad\
├─ convert_to_md.py
├─ requirements.txt
├─ requirements_mineru.txt
├─ 标准2\
└─ .venv\
```

模型目录使用：

```text
D:\mineru\models
```

### 2. 创建 Python 虚拟环境

在 PowerShell 中执行：

```powershell
Set-Location "D:\chemical_design\test\standrad"

uv venv .venv --python 3.12
```

激活虚拟环境：

```powershell
.\.venv\Scripts\Activate.ps1
```

如果 PowerShell 禁止执行脚本，可先执行：

```powershell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
```

### 3. 安装基础依赖

```powershell
uv pip install --python ".venv\Scripts\python.exe" -r requirements.txt
```

### 4. 安装 MinerU

```powershell
uv pip install --python ".venv\Scripts\python.exe" -r requirements_mineru.txt
```

安装完成后检查：

```powershell
mineru --version
mineru-kit --help
```

### 5. 设置模型目录和模型源

模型不要放 C 盘时，先设置：

```powershell
$env:MINERU_MODEL_BASE_DIR = "D:\mineru\models"
$env:MINERU_MODEL_SOURCE = "modelscope"
```

建议永久写入用户环境变量：

```powershell
[Environment]::SetEnvironmentVariable(
  "MINERU_MODEL_BASE_DIR",
  "D:\mineru\models",
  "User"
)

[Environment]::SetEnvironmentVariable(
  "MINERU_MODEL_SOURCE",
  "modelscope",
  "User"
)
```

重新打开 PowerShell 后检查：

```powershell
mineru-kit models show
```

应看到：

```text
model.base_dir: D:\mineru\models
model.base_dir.source: env
model.source: modelscope
```

### 6. 下载 standard 档模型

```powershell
mineru-kit models download --tier standard --source modelscope -v
```

下载内容：

```text
MinerU-4_models_onnx
MinerU2.5-Pro-2605-1.2B-GGUF
```

验证：

```powershell
mineru-kit models verify --tier standard
```

期望：

```text
MinerU-4_models_onnx: ok
MinerU2.5-Pro-2605-1.2B-GGUF: ok
```

### 7. 启用本地 managed 模式

```powershell
mineru config set parse_server.local.mode managed
mineru config set parse_server.local.managed_tier standard
```

重启服务：

```powershell
mineru server stop
mineru server start
mineru server status
```

检查 Parse Server 表格：

```text
本地 (managed)  健康=是  档位=standard
```

第一次启动会加载 ONNX 和 GGUF 模型，可能需要几分钟。

---

## 三、每次运行前需要的指令

### 1. 打开 PowerShell，进入项目目录

```powershell
Set-Location "D:\chemical_design\test\standrad"
```

### 2. 设置模型路径（如果没写入永久环境变量）

```powershell
$env:MINERU_MODEL_BASE_DIR = "D:\mineru\models"
$env:MINERU_MODEL_SOURCE = "modelscope"
```

### 3. 激活虚拟环境

```powershell
.\.venv\Scripts\Activate.ps1
```

确认 Python：

```powershell
python -c "import sys; print(sys.executable)"
```

应输出：

```text
D:\chemical_design\test\standrad\.venv\Scripts\python.exe
```

### 4. 检查 MinerU 服务

```powershell
mineru server status
```

重点看：

```text
本地 (managed)  健康=是  档位=standard
```

如果本地不是“健康=是”，执行：

```powershell
mineru server start
```

再次检查：

```powershell
mineru server status
```

### 5. 可选：先小批量试运行

只转换“应急”子目录：

```powershell
$BASE = "D:\chemical_design\test\standrad"

python convert_to_md.py `
  --source "$BASE\标准2\应急" `
  --output "$BASE\_trial_应急_mineru" `
  --pdf-engine auto `
  --mineru-tier standard `
  --overwrite
```

检查公式：

```powershell
Select-String -Path "$BASE\_trial_应急_mineru\*.md" -Pattern '\$\$|\$|\\frac|\\sqrt|\\alpha|\\beta'
```

### 6. 全量转换

```powershell
$BASE = "D:\chemical_design\test\standrad"

python convert_to_md.py `
  --source "$BASE\标准2" `
  --output "$BASE\标准2_md" `
  --pdf-engine auto `
  --mineru-tier standard `
  --overwrite
```

说明：

- `--pdf-engine auto`：优先使用 MinerU，支持公式/表格/版面识别；
- `--mineru-tier standard`：standard 档，公式识别效果更好；
- `--overwrite`：覆盖已有 md；
- 如果不想覆盖，去掉 `--overwrite`，程序会跳过已存在的 md。

### 7. 查看结果

打开复核页面：

```powershell
Invoke-Item "$BASE\标准2_md\_qa\review_index.html"
```

查看报告：

```powershell
Get-Content "$BASE\标准2_md\_qa\qa_report.csv"
Get-Content "$BASE\标准2_md\_qa\review_queue.csv"
```

---

## 四、参数说明

| 参数 | 说明 |
|---|---|
| `--source` | 源目录 |
| `--output` | 输出目录 |
| `--pdf-engine auto` | 优先 MinerU，失败再降级 |
| `--pdf-engine mineru` | 强制所有 PDF 走 MinerU |
| `--pdf-engine pymupdf` | 只做数字文本提取，速度快，但公式可能无法识别 |
| `--mineru-tier standard` | 使用标准档，公式识别推荐 |
| `--mineru-tier advanced` | 更高档位，要求本地模型支持 advanced |
| `--overwrite` | 覆盖已有输出 |
| `--no-resume` | 不跳过已有输出 |
| `--limit 3` | 只处理前 3 个文件 |
| `--dry-run` | 只列出文件，不转换 |
| `--list-engines` | 查看本机转换引擎 |

---

## 五、输出结构

源目录：

```text
标准2\
├─ 危化\
├─ 双重预防\
├─ 应急\
├─ 法律\
└─ 特殊作业\
```

输出目录：

```text
标准2_md\
├─ 危化\
│  ├─ xxx.md
│  └─ xxx_assets\
├─ 双重预防\
├─ 应急\
├─ 法律\
├─ 特殊作业\
└─ _qa\
   ├─ qa_report.csv
   ├─ review_queue.csv
   ├─ review_index.html
   └─ convert_to_md.log
```

每个源文件对应一个独立 md，不会把所有内容合并到一个文件里。

---

## 六、常见问题

### 1. 公式没有输出

检查：

```powershell
mineru server status
```

必须满足：

```text
本地 (managed)  健康=是  档位=standard
```

然后转换时必须带：

```powershell
--pdf-engine auto --mineru-tier standard
```

不要用：

```powershell
--pdf-engine pymupdf
```

`pymupdf` 只提取文字，不做公式识别。

### 2. 报错：No mapping for the Unicode character exists in the target multi-byte code page

原因：项目或虚拟环境路径包含中文。  
处理：把项目移动到全英文路径，例如：

```text
D:\chemical_design\test\standrad
```

不要在中文路径下创建 `.venv`。

### 3. 本地 Parse Server 健康=否

先查看状态：

```powershell
mineru server status
```

然后看日志：

```text
C:\Users\Administrator\.mineru\logs\doclib.log
C:\Users\Administrator\.mineru\logs\doclib.parse-server.stderr.log
C:\Users\Administrator\.mineru\logs\doclib.parse-server.stdout.log
```

重启：

```powershell
mineru server stop
mineru server start
mineru server status
```

### 4. 模型文件缺失

重新下载：

```powershell
mineru-kit models download --tier standard --source modelscope -v
mineru-kit models verify --tier standard
```

### 5. 想换模型目录

设置：

```powershell
$env:MINERU_MODEL_BASE_DIR = "E:\mineru\models"
$env:MINERU_MODEL_SOURCE = "modelscope"
```

然后重新下载模型：

```powershell
mineru-kit models download --tier standard --source modelscope -v
```

最后重启：

```powershell
mineru server stop
mineru server start
mineru server status
```

---

## 七、最简每次运行命令

如果模型路径已经永久写入环境变量，每次只需要：

```powershell
Set-Location "D:\chemical_design\test\standrad"
.\.venv\Scripts\Activate.ps1

mineru server start
mineru server status

python convert_to_md.py `
  --source "D:\chemical_design\test\standrad\标准2" `
  --output "D:\chemical_design\test\standrad\标准2_md" `
  --pdf-engine auto `
  --mineru-tier standard `
  --overwrite
```

如果只想处理新增文件，去掉 `--overwrite`。

---

## 八、正式验收建议

1. 打开 `标准2_md\_qa\review_index.html`；
2. 所有红色行都需要人工复核；
3. 扫描件重点检查：数字、单位、上下标、公式、表格、标准编号；
4. 确认没有缺页、乱码、公式断裂；
5. 复核完成后再把 `标准2_md` 作为最终结果。
